# سياسة الأمان

## الإصدارات المدعومة

يحصل أحدث إصدار منشور فقط على إصلاحات الأمان. الإضافة في مرحلة Release
Candidate، لذا يجب اختبار الترقية على بيئة غير إنتاجية وأخذ نسخة احتياطية قابلة
للاستعادة قبل كل تحديث.

## الإبلاغ عن ثغرة

استخدم **Private vulnerability reporting** من تبويب Security في مستودع GitHub.
لا تنشئ Issue عامًا لثغرة غير معالجة، ولا ترفق بيانات طالبات أو شهادات أو ملفات
بيئة أو سجلات تحتوي معرفات شخصية.

أرفق وصف الأثر، والإصدار المتأثر، وخطوات إعادة إنتاج مختصرة ببيانات وهمية، وأي
اقتراح للإصلاح. سيُراجع البلاغ وتُنشر المعالجة والإفصاح بعد توفر إصدار آمن.
