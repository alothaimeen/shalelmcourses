# المساهمة في الإضافة

شكرًا للمساهمة في `local_shomokh_admissions`. يستهدف هذا الفرع Moodle 4.5
وPHP 8.1–8.3.

## قبل إرسال التغيير

1. أنشئ فرعًا مستقلًا ولا تضع أسرارًا أو بيانات طالبات أو شهادات في Git.
2. التزم بمعيار Moodle للكود، واستخدم Moodle APIs بدل الوصول المباشر للملفات
   أو تعديل Moodle Core.
3. أضف اختبار PHPUnit لكل تغيير في منطق القبول أو الأهلية أو الإلحاق أو الخصوصية.
4. حدّث مفاتيح اللغتين العربية والإنجليزية معًا، وحدّث `CHANGELOG.md` عند تغير
   السلوك المرئي أو قاعدة البيانات.
5. نفّذ، عبر `moodle-plugin-ci`، فحوص `phplint` و`phpcs` و`savepoints`
   و`phpunit` قبل فتح Pull Request.

لا نشغّل فحص `moodle-plugin-ci validate` العام؛ لأن الإضافة ثُبتت فعليًا
ببادئة الجداول المختصرة والثابتة `local_shadm_`، بينما يفترض هذا الفحص أن تبدأ
كل الجداول باسم المكوّن الكامل. لا تُعد تسمية جداول قاعدة البيانات الموجودة
لمجرد إرضاء هذا الافتراض؛ وتظل فحوص التثبيت، والتوثيق، ونمط الكود، ونقاط
الحفظ، وPHPUnit إلزامية.

## الإبلاغ عن المشكلات

اشرح الخطوات والنتيجة المتوقعة والفعلية وإصدارات Moodle وPHP وقاعدة البيانات.
استخدم بيانات وهمية واحذف أسماء الطالبات والبريد وأرقام الهاتف ومحتوى الشهادات.
لا تنشر الثغرات الأمنية في Issue عام؛ اتبع `SECURITY.md`.
